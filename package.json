{
  "name": "slimgpt",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --mode chrome",
    "build": "npm run build:extension && npm run build:firefox && npm run build:orion",
    "build:extension": "node scripts/build.mjs chrome",
    "build:firefox": "node scripts/build.mjs firefox",
    "build:orion": "node scripts/build.mjs orion",
    "live:smoke": "npm run build:extension && node scripts/live-smoke.mjs",
    "live:smoke:real": "npm run build:extension && node scripts/live-smoke.mjs --live",
    "test": "node --test tests/*.test.js",
    "check": "node --check main-mitm.js && node --check isolated-bridge.js && node --check core.js && node --check markdown-worker.js && node --check scripts/build.mjs && node --check scripts/live-smoke.mjs && node --check scripts/visual-verify.mjs && node -e \"const fs=require('node:fs'); for (const f of ['manifest.json','manifest.firefox.json','manifest.orion.json']) JSON.parse(fs.readFileSync(f,'utf8'))\""
  },
  "dependencies": {
    "framework7": "9.1.3",
    "framework7-svelte": "9.1.3",
    "highlight.js": "11.12.0",
    "smol-toml": "1.8.0",
    "svelte": "5.57.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "7.3.0",
    "vite": "8.2.2"
  }
}
